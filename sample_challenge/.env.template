# Port for Agentish UI (default: 8000)
AGENTISH_PORT=8000

# Configuration files are managed through model_config.yaml and mcp_config.yaml
# No additional environment variables are needed here for runtime configuration
#
# Note: All LLM provider settings (llamacpp, litellm, openai), Langfuse configuration,
# and MCP server settings are now configured through the YAML files
